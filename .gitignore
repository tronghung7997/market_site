.env
docker-compose.yml
pgdata/
.idea
.playwright-mcp
docker-compose.dev.yml
.backend.env
pyproject.toml
.agents
AGENTS.md
CONTEXT.md
docs

# `docs/` is otherwise local scratch space. This is the versioned dispute
# settlement contract used by buyer, seller and operations flows.
!docs/
docs/*
!docs/dispute-flow.md

# Coding-agent files are repository contracts and must be versioned.
!AGENTS.md
!frontend/AGENTS.md
!marketplace-svc/AGENTS.md
!CONTEXT.md
.pi
scratch
.hallmark
frontend/CLAUDE.md
skills-lock.json
/frontend/frontend.env

# Ad-hoc environment backups taken during E2E/debug sessions. These are full
# dev env dumps containing real provider credentials; they must never be
# committed, and the untracked-but-not-ignored state made `git add -A` risky.
*.env.bak-*
.env.bak-*
