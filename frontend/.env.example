NEXT_PUBLIC_API_URL=/api
API_URL=http://localhost:8001
TIKTOK_LOOKUP_API_URL=https://lookup.ghlab.info/api/v1/tiktok
# Keep this value raw. A slash in the provider key is `/`, not `%2F`.
TIKTOK_LOOKUP_API_KEY=replace-with-provider-key
# Only set these when the Next runtime reaches the provider through an outbound HTTP proxy.
# NODE_USE_ENV_PROXY=1
# HTTP_PROXY=http://proxy.internal:8080
# HTTPS_PROXY=http://proxy.internal:8080
